{
	"private": true,
	"scripts": {
		"build": "parcel build 'source/**/*.html' 'source/**/*.pug' 'source/favicon.ico' --no-cache",
		"start": "parcel serve 'source/**/*.html' 'source/**/*.pug' --no-cache",
		"mavo": "browser-sync start --server --files '*'"
	},
	"devDependencies": {
		"@parcel/transformer-pug": "^2.0.0-nightly.678",
		"modern-normalize": "^1.1.0",
		"parcel": "^2.0.0-beta.3.1",
		"posthtml-img-autosize": "^0.1.6"
	},
	"browserslist": "defaults",
	"dependencies": {
		"animate-prop": "^1.0.0"
	}
}
