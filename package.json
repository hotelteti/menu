{
	"private": true,
	"scripts": {
		"build": "parcel build 'source/**/*.html' --no-cache",
		"start": "parcel serve 'source/**/*.html' --no-cache",
		"mavo": "browser-sync start --server --files '*'"
	},
	"devDependencies": {
		"modern-normalize": "^1.1.0",
		"parcel": "^2.0.0-beta.3.1",
		"posthtml-expressions": "^1.7.1",
		"posthtml-img-autosize": "^0.1.6"
	}
}
